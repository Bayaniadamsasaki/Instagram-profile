import 'package:flutter/material.dart';

class ButtomTab extends StatelessWidget {
  const ButtomTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}